export type UserAppBarInfo = {
    name: string;
    photo: string;
}

export type User = {
    id: number;
    name: string;
    email: string;
    birthDate: string;
    documentType: string;
    documentNumber: string;
    url_photo: string;
}